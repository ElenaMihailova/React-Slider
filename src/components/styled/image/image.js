import styled from 'styled-components';

const Image = styled.img`
  display: flex;
  margin: 0 auto;
  aspect-ratio: 668/1023;
  width: 100%;
  max-height: 100%;
  object-fit: contain;
`;

export default Image;
